fn main() {
    let mut x: i32 =1;
    x+=2;
    assert_eq!(x,3);
    println!("Program is run ");
  }

/*

1.here "mut" is a keyword use for making variables mutable
2.by default variables are immutable.
3. [x+=2] is simply represent [x=x+2]

*/